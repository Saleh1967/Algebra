"""جبرٌ قائمٌ بذاته: ما يُبرهَن بالحساب، لا يستورد من شجرةٍ أخرى.

**ما هذه الحزمة**: ثلاثةُ بناءاتٍ رياضيّةٍ لا تعرف عربيّةً ولا جذرًا ولا
مدوَّنة. كلُّ واحدٍ منها خرج من عملٍ على مشروعٍ آخَر، ثمّ **جُرِّد** ممّا يخصّ
ذلك المشروعَ حتى صار يقوم وحدَه:

* `simplicial` — المركّباتُ البسيطةُ على أيّ مجموعةِ رؤوسٍ منتهية، وحدُّها
  **مؤشَّر**، وأعدادُ بيتي بكسورٍ صحيحة، ومَعيارُ أويلر فحصًا مستقلًّا.
* `decisions` — سجلُّ قراراتٍ بأنواعٍ مغلقةٍ وشروطِ إنشاء: فرعٌ بلا ثمنٍ لا
  يُقيَّد، ومُفوَّضٌ يُفرَّق فيه مَن يملك القرارَ مِن مَن اختار.
* `attainability` — هل يبلغ اختبارٌ حدَّه أصلًا؟ أرضيّةُ التباديل، ومخارجُ
  الرفض المتاحة، وأدنى عددِ تكرارٍ لحدٍّ مُعلَن.

`THIS_PACKAGE_IMPORTS_NOTHING_FROM_ALGHANEM`: لا وحدةَ هنا تستورد من
`alghanem`، ولا تقرأ بايتاتِ مدوَّنةٍ، ولا تحمل رقمًا من قياسٍ لغويّ. وما في
`src/alghanem` من هذا المستودع **إيداعُ نقلٍ** منفصلٌ عن هذه الحزمة تمامًا،
مشروحٌ في `src/alghanem/TRANSFER_NOTICE.md`.

`A_GENERALIZATION_IS_NOT_A_COPY`: ما نُقِل ههنا وُسِّع وقُيِّد معًا: المركّباتُ
صارت على `n` رأسًا لا ثلاثةً فقط، وسجلُّ القرارات صارت سلطاتُه المرفوضةُ
معطًى لا قائمةً مكتوبةً في متنه، وحسابُ البلوغ صار على أيّ `(α, B)` لا على
حالةٍ واحدة. والنسخُ حرفًا بحرفٍ كان يصنع تابعًا لا مشروعًا.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
