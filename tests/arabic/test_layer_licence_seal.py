"""**ختمٌ قبل النظر**: البناءُ صاعدًا من الـ١١٢، ولا صعودَ إلّا برخصة.

**لم يُشغَّل شيءٌ بعد.**

**شرطُ الرخصة — أربعةٌ مجتمعة، وسقوطُ واحدٍ يوقف الصعود**:

1. **الدالّية**: الخريطةُ من الطبقة إلى ما فوقها **دالّةٌ كلّيّة** — كلُّ
   وحدةٍ تُسنَد، وإلى واحدةٍ لا أكثر. والعلاقةُ لا تُرخَّص.
2. **الانغلاق**: أبجديّةُ الطبقة الأعلى **منتهيةٌ محصاةٌ بالعدّ** على
   المجمَّد، لا مقدَّرةٌ ولا مفتوحة.
3. **التمام**: مجموعُ ما أُسنِد **يُغلِق** عددَ الطبقة الأدنى بلا بقيّة،
   وكلُّ وحدةٍ غيرِ مُسنَدةٍ **تُعَدّ باسمها ولا تُبتلَع**.
4. **الرجعة**: الطبقةُ الأدنى تُستعاد من الأعلى **مع بقيّةٍ مسمّاةٍ
   معدودةِ البتّات محجوزةً** — فلا صعودَ بلا ثمنٍ معلوم.

**الطبقاتُ المقترحة** (وكلُّ واحدةٍ تُفحَص على الأربعة قبل التي فوقها):

- **L₀ الوحدةُ**: صامتٌ + حركة، ١١٢، مثبَتةٌ في `c5a1e18`.
- **L₁ المقطعُ**: داخلَ الكلمة، الوحدةُ ذاتُ الحركة القصيرة **تفتح** مقطعًا،
  والساكنةُ التالية **تُغلِقه** قيدًا واحدًا. ونوعُه `CV` بلا غلق، و`CVV`
  إن كان الغالقُ حرفَ مدٍّ **موافقًا** للحركة (ا↔فتحة، و↔ضمة، ي↔كسرة)،
  و`CVC` فيما سواه. وساكنٌ بلا مقطعٍ مفتوحٍ قبله، أو ساكنٌ ثانٍ بعد غلق،
  **غيرُ مُرخَّصٍ ويُعَدّ**.
- **L₂ شكلُ الكلمة**: تسلسلُ أنواع مقاطعها.
- **L₃ وزنُ الكلمة الكمّيّ**: إسقاطُ الشكل على {خفيف = CV، ثقيل = ما سواه}.
- **L₄ فما فوقُ**: يُبلَغ إن رُخِّص، ويُوقَف باسمه إن لم يُرخَّص.

**والقياسُ محجوزٌ**: الآياتُ الزوجيّةُ سندًا والفرديّةُ قياسًا ثمّ بالعكس،
بتنعيم لابلاس على أبجديّة الطبقة وحدَها.
"""

from __future__ import annotations

from fractions import Fraction

from algebra.signified import Direction, Oracle, Prediction, seal

ORACLE = Oracle(
    name="البناءُ الصاعدُ من الـ١١٢ برخصةٍ مقيسة",
    source="quran-simple-enhanced.txt — البصمة 37633090…",
    extraction=(
        "من وحدات L₀ المثبَتة في `c5a1e18` تُبنى L₁ بقاعدة المقطع المعلنة، "
        "ثمّ L₂ شكلًا وL₃ وزنًا؛ وتُفحَص كلُّ طبقةٍ على أربعة: الدالّية "
        "والانغلاق والتمام والرجعة؛ والبقايا إنتروبيا متقاطعةٌ محجوزةٌ "
        "بقسمة الآيات الزوجيّة/الفرديّة وتنعيمِ لابلاس على أبجديّة الطبقة"
    ),
)

PREDICTIONS = (
    Prediction(
        identifier="ه١",
        statistic="نصيبُ وحدات L₀ غيرِ المُسنَدةِ إلى مقطعٍ في L₁",
        threshold=Fraction(1, 100),
        direction=Direction.AT_MOST,
        falsifies=(
            "شرطَ الدالّيةِ لـL₁: قاعدةُ المقطع المعلنةُ ليست كلّيّةً على "
            "المدوّنة، فيوقَف الصعودُ عندها وتُعاد صياغةُ القاعدة بختمٍ جديد"
        ),
    ),
    Prediction(
        identifier="ه٢",
        statistic="عددُ أنواع المقاطع المرصودةِ في L₁",
        threshold=Fraction(8),
        direction=Direction.AT_MOST,
        falsifies=(
            "شرطَ الانغلاق: إن جاوزت الأنواعُ ثمانيةً فالقاعدةُ تولّد "
            "أصنافًا لم تُعلَن، ويُعاد حصرُها قبل أيّ صعود"
        ),
    ),
    Prediction(
        identifier="ه٣",
        statistic="أقصى فرقٍ بين مجموع المُسنَد وعددِ الطبقة الأدنى (وحدات)",
        threshold=Fraction(0),
        direction=Direction.AT_MOST,
        falsifies=(
            "شرطَ التمام: بقيّةٌ في الجمع تعني ابتلاعًا صامتًا، وهو الجنسُ "
            "السادسُ من الأعطال بعينه"
        ),
    ),
    Prediction(
        identifier="ه٤",
        statistic="عددُ الطبقات المُرخَّصةِ فوق L₀ من المدوّنة وحدَها",
        threshold=Fraction(3),
        direction=Direction.AT_LEAST,
        falsifies=(
            "دعوايَ أنّ البناءَ يبلغ الوزنَ الكمّيَّ بلا إيداعٍ خارجيّ؛ "
            "فإن وقف دونها فالسقفُ أدنى ممّا قدّرتُ ويُنشَر كذلك"
        ),
    ),
    Prediction(
        identifier="ه٥",
        statistic="أدنى (البقيّة المحجوزة − البقيّة الملحَقة) على الطبقات (بت)",
        threshold=Fraction(0),
        direction=Direction.AT_LEAST,
        falsifies="اتّجاهَ الانتحال؛ وهو الدرسُ الذي سقطت به نتيجتي في `ca8fd2c`",
    ),
    Prediction(
        identifier="ه٦",
        statistic="نصيبُ بقيّةِ الرجعة من L₁ إلى L₀ من إفادة L₀",
        threshold=Fraction(1, 2),
        direction=Direction.AT_MOST,
        falsifies=(
            "دعوى أنّ المقطعَ طبقةٌ نافعة: إن كلّف ردُّه إلى الوحدات أكثرَ "
            "من نصف إفادتها فهو إعادةُ ترتيبٍ لا تجريد، ولا يُرخَّص صعودًا"
        ),
    ),
)

DIGEST = "73cc5b246fa495526b02b4f6f5f77af1308178f397ead25aaa3541293490d7f1"
"""يُثبَّت بعد أوّل اشتقاق، ولا يُمَسّ بعدَه."""


def test_the_seal_is_recomputed_from_the_written_conditions() -> None:
    """البصمةُ تُشتَقّ من الشروط لا تُنقَل."""

    assert seal(ORACLE, PREDICTIONS) == DIGEST


def test_the_licence_has_four_clauses_and_each_has_a_condition() -> None:
    """الدالّيةُ والانغلاقُ والتمامُ والرجعةُ — ولكلٍّ شرطٌ يُسقِطه."""

    clauses = {"ه١": "الدالّية", "ه٢": "الانغلاق", "ه٣": "التمام", "ه٦": "الرجعة"}
    named = {one.identifier for one in PREDICTIONS}
    assert set(clauses) <= named
    assert len(PREDICTIONS) == 6
    assert len(named) == 6


def test_one_condition_is_aimed_at_my_own_forecast() -> None:
    """ه٤ يُعرّض تقديري أنا لعدد الطبقات، لا تقديرَ غيري."""

    mine = next(one for one in PREDICTIONS if one.identifier == "ه٤")
    assert "دعوايَ" in mine.falsifies
    assert mine.threshold == Fraction(3)
