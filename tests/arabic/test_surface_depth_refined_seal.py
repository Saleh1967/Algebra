"""**ختمٌ ثانٍ قبل النظر**: أصنافُ العُري بترتيبٍ مُصحَّح — ولم يُشغَّل الأوّلُ بعد.

**لم يُشغَّل شيءٌ بعد**، لا هذا ولا ختمُ `945fd806…`.

`I_FOUND_THE_DEFECT_BY_READING_MY_RULES_NOT_BY_READING_COUNTS`: ختمُ
`945fd806…` يقدّم صنفَ المدّ (س١) على صنفِ ألفِ الوصل (س٢)، فألفُ «وَالنَّاسِ»
و«فَاضْرِبْ» و«كَالْأَنْعَامِ» — وقبلها حرفٌ عليه **فتحة** — تُعطى المدَّ، فلا
تكون ألفَ س٢، **فتسقط لامُ «ال» بعدها في الباقي**. عرفتُ ذلك من قراءة
قواعدي لا من عدٍّ، فلم يُعَدّ شيء. **ولا يُبدَّل ذلك الختم**: يُشغَّل كما
خُتِم ويُنشَر بعلّته، وهذا الختمُ **بجانبه** لا مكانَه.

`AND_ONE_SAMPLE_WAS_SEEN_BEFORE_BOTH_SEALS`: وقبل الختمين طبعتُ خمسةَ
سياقاتٍ للوسم `<sel>` لأعرف ما هو، وفيها «وَلَهُم <sel>» **بميمٍ عارية** قبل
الوسم، و«رَبِّهِمْ <sel>» بميمٍ عليها سكون. **فهذه عيّنةٌ رُئيت**، وهي تمسّ
شرطَ الميم. فيُعلَن ههنا، ويُقاس التالي في هذا الختم **دون عبور الوسم**:
الوسمُ موضعُ وقفٍ جائز، فلا تاليَ مقطوعًا به بعده — وهذا تعريفٌ يُختار
**بعد** رؤية العيّنة، فيُعلَن أنّه اختير بعدها.

**والتصحيح**:

| الصنف | النمط | القراءة (غيرُ موقَّعة) |
|---|---|---|
| ق١ | ا في ٠، أو في ١–٢ بعد سوابقَ متحرّكة وقبل غيرِ متحرّك | همزةُ وصل |
| ق٢ | ل عاريةٌ تلي ألفَ ق١ مباشرةً ويليها مشدّد | لامٌ شمسيّة |
| ق٣ | ا/ى بعد فتحة، و بعد ضمّة، ي بعد كسرة (والتنوينُ كحركته) | مدّ |
| ق٤ | ن أو م عارية | نونٌ أو ميمٌ لم يُكتَب سكونُها |
| ق٥ | ا آخرَ اللفظ بعد و لا حركةَ عليها | الألفُ الفارقة |
| ق٦ | آ عارية | ألفٌ ممدودة |

**وثمنُ التصحيح مُعلَنٌ قبل العدّ**: «كَافَّة» و«دَابَّة» — ألفٌ في الموضع
الأوّل بعد حرفٍ متحرّك ويليها مشدّد — تقع في ق١ وهي مدّ. «دَابَّة» لا تدخل
لأنّ الدالَ ليست من السوابق، و«كَافَّة» تدخل. **وهذا خطأُ تصنيفٍ بين صنفين
لا يمسّ الباقي**؛ ويُسرَد عددُه في التشغيل بالاسم لا يُطوى.
"""

from __future__ import annotations

from fractions import Fraction

from algebra.signified import Direction, Oracle, Prediction, seal

ORACLE = Oracle(
    name="السطحُ والعمقُ صعودًا من الـ١١٢ — أصنافُ العُري بترتيبٍ مُصحَّح",
    source="quran-simple-enhanced.txt — البصمة 37633090…",
    extraction=(
        "يُطرَح الوسمُ <sel> فراغًا للّفظ ويُحفَظ حدًّا للتالي؛ واللفظُ ما "
        "فصلته الفراغات؛ والحرفُ ما كان في الثمانية والعشرين أو أسرة الألف "
        "(ء أ إ آ ؤ ئ ى) أو ة؛ والعلامةُ ما تلا الحرفَ من فتحةٍ وضمّةٍ وكسرةٍ "
        "وسكونٍ وشدّةٍ وتنوين؛ والعاري حرفٌ لم تتلُه علامةٌ البتّة؛ والمتحرّكُ "
        "ما عليه فتحةٌ أو ضمّةٌ أو كسرةٌ أو تنوين؛ وحركةُ الحرف السابق آخرُ "
        "علامةِ حركةٍ أو تنوينٍ عليه في اللفظ نفسه؛ والأصنافُ بالترتيب وأوّلُ "
        "مطابقٍ يحكم: ق١ ا في الموضع ٠، أو في الموضع ١ أو ٢ وكلُّ ما قبلها "
        "من (و ف ب ك ل أ) متحرّكٌ والحرفُ التالي في اللفظ موجودٌ وعارٍ أو "
        "عليه سكونٌ أو شدّة؛ ق٢ ل تلي ألفَ ق١ مباشرةً ويليها حرفٌ عليه شدّة؛ "
        "ق٣ ا/ى بعد فتحةٍ أو تنوين فتح، وو بعد ضمّةٍ أو تنوين ضمّ، وي بعد "
        "كسرةٍ أو تنوين كسر؛ ق٤ ن أو م؛ ق٥ ا آخرَ اللفظ بعد و غيرِ متحرّكة؛ "
        "ق٦ آ؛ وما عداها باقٍ؛ والحرفُ التالي أوّلُ حرفٍ بعده في ترتيب "
        "القراءة عابرًا الفراغ، ولا يعبر الوسمَ ولا آخرَ السطر، فما لا تاليَ "
        "له يُعَدّ ويُستثنى من مقام ج٤ وج٥ وج٦ وج٧؛ وحروفُ الحلق ء أ إ آ ؤ ئ "
        "هـ ع ح غ خ؛ والمطابقةُ بايتةً بايتة بتقشير run_cv_peel سطرًا سطرًا؛ "
        "والنافذةُ ٦٢٢٢–٦٢٣٦ وعيّنةُ الوسم الخمسُ رُئيتا قبل الختم فلا تدخلان "
        "شرطًا"
    ),
)

PREDICTIONS = (
    Prediction(
        identifier="ج١",
        statistic="أسطرٌ لا يعيدها rebuild(peel(سطر)) بايتةً بايتة",
        threshold=Fraction(0),
        direction=Direction.AT_MOST,
        falsifies=(
            "الآلةَ لا المادّة: إن لم تُعَد البايتاتُ من الـ١١٢ فالقاعُ الذي "
            "يُصعَد منه مثقوب، ويُردّ كلُّ ما فوقه"
        ),
    ),
    Prediction(
        identifier="ج٢",
        statistic="|مجموعُ الأصناف ق١…ق٦ والباقي − عددُ العُري|",
        threshold=Fraction(0),
        direction=Direction.AT_MOST,
        falsifies=(
            "الآلةَ: الأصنافُ مفصولةٌ بالترتيب وجامعةٌ بالباقي، فاختلافُ "
            "المجموع عطلُ حسابٍ يردّ الجدول"
        ),
    ),
    Prediction(
        identifier="ج٣",
        statistic="نصيبُ الباقي من العُري كلِّه",
        threshold=Fraction(1, 50),
        direction=Direction.AT_MOST,
        falsifies=(
            "قولي إنّ العُريَ أصنافٌ لا عشواء: إن جاوز الباقي جزءًا من خمسين "
            "بعد التصحيح فالأنماطُ الستّةُ لا تستوعب الظاهرة، والخللُ ليس "
            "الترتيبَ وحدَه"
        ),
    ),
    Prediction(
        identifier="ج٤",
        statistic="نصيبُ «ن» العارية التي تاليها حرفُ حلق، ممّا له تالٍ",
        threshold=Fraction(1, 100),
        direction=Direction.AT_MOST,
        falsifies=(
            "قولي إنّ النونَ العارية نونٌ أُخفيت أو أُدغمت: إن جاورت حرفَ "
            "الحلق في أكثرَ من واحدٍ من مئة فالعُريُ فيها ليس أثرَ ما بعدها"
        ),
    ),
    Prediction(
        identifier="ج٥",
        statistic="نصيبُ «م» العارية التي تاليها غيرُ ب ولا م، ممّا له تالٍ",
        threshold=Fraction(1, 100),
        direction=Direction.AT_MOST,
        falsifies=(
            "القولَ نفسَه في الميم، على تعريف التالي الذي لا يعبر الوسم: إن "
            "عريت الميمُ قبل غير الباء والميم في أكثرَ من واحدٍ من مئة "
            "فعُريُها ليس أثرَ ما بعدها"
        ),
    ),
    Prediction(
        identifier="ج٦",
        statistic="نصيبُ «ل» العارية التالية لألف ق١ ولا يليها حرفٌ عليه شدّة",
        threshold=Fraction(1, 100),
        direction=Direction.AT_MOST,
        falsifies=(
            "قولي إنّ لامَ «ال» لا تعرى إلّا مُدغَمةً في ما بعدها: إن عريت "
            "قبل غير المشدّد في أكثرَ من واحدٍ من مئة فالعُريُ فيها ليس إدغامًا"
        ),
    ),
    Prediction(
        identifier="ج٧",
        statistic=("نصيبُ «ل» التالية لألف ق١ ويليها حرفٌ عليه شدّة وعليها هي علامة"),
        threshold=Fraction(1, 100),
        direction=Direction.AT_MOST,
        falsifies=(
            "العكسَ: إن كُتبت علامةٌ على لام «ال» قبل المشدّد في أكثرَ من "
            "واحدٍ من مئة فليس الإدغامُ هو ما يُعرّيها، وج٦ وحدَه لا يكفي"
        ),
    ),
)

DIGEST = "34176d22aa61d588e34ab80d7671471b2b916eb193cc0240b0f7afd9c38e5157"
"""يُثبَّت بعد أوّل اشتقاق، ولا يُمَسّ بعدَه."""

FIRST_SEAL = "945fd8066f2e0ee6aebe6c93e717f9b69dcabd67fac705891551b765d048972b"
"""الختمُ الأوّلُ الذي يقف هذا بجانبه لا مكانَه."""


def test_the_seal_is_recomputed_from_the_written_conditions() -> None:
    """البصمةُ تُشتَقّ من الشروط لا تُنقَل."""

    assert seal(ORACLE, PREDICTIONS) == DIGEST


def test_the_first_seal_is_untouched_beside_this_one() -> None:
    """الختمُ الأوّلُ يُعاد اشتقاقُه كما هو — ولم يُبدَّل حرفٌ فيه."""

    from test_surface_depth_seal import DIGEST as FIRST
    from test_surface_depth_seal import ORACLE as FIRST_ORACLE
    from test_surface_depth_seal import PREDICTIONS as FIRST_PREDICTIONS

    assert FIRST == FIRST_SEAL
    assert seal(FIRST_ORACLE, FIRST_PREDICTIONS) == FIRST_SEAL
    assert DIGEST != FIRST_SEAL


def test_the_only_changes_are_the_order_and_the_follower() -> None:
    """الشروطُ السبعةُ نفسُها مقاييسَ وحدودًا — تغيّر الترتيبُ وتعريفُ التالي."""

    from test_surface_depth_seal import PREDICTIONS as FIRST_PREDICTIONS

    for mine, theirs in zip(PREDICTIONS, FIRST_PREDICTIONS, strict=True):
        assert mine.identifier == theirs.identifier
        assert mine.threshold == theirs.threshold
        assert mine.direction is theirs.direction
    assert "ولا يعبر الوسمَ" in ORACLE.extraction
    assert "رُئيتا قبل الختم" in ORACLE.extraction
